<%= form_with(model: @speaker, local: true) do |form| %>
  
<% if @speaker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@speaker.errors.count, "error") %> prohibited this speaker from being saved:</h2>

      <ul>
        <% @speaker.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="fields">
    <%= form.label :user %>
    <%= form.collection_select :user_id, User.all, :id, :name, prompt: true %>
  </div>

  <div class="fields">
    <%= form.label :user %>
    <%= form.collection_select :talk_id, Talk.all, :id, :name, prompt: true %>
  </div>
  <div class="actions">
    <%= form.submit 'Criar Palestra' %>
  </div>
<% end %>