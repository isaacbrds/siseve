<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Siseve</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if current_user %>
                <li class="nav-item px-3">
                        <%= link_to "#{t('Events')}", events_path , class: "dropdown-item"%>
                </li>
                <li class="nav-item">
                        
                </li>
                <% end %>
                
            </ul>
            <% if current_user %> 
                <ul class="navbar-nav">
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false"> 
                            
                            <%= current_user.email %> 
                        
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            
                            <li>
                                <%= link_to t('edit'),edit_user_registration_path(current_user), class: "dropdown-item" %>
                            </li>
                            <li><hr class="dropdown-divider" /></li>
                            <li>
                                <%= link_to t('logout'), destroy_user_session_path, data: { "turbo-method": :delete }, class: "dropdown-item" %>
                            </li>
                    </li>
                </ul>
                <% else %>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <%= link_to 'Login', new_user_session_path, class: "dropdown-item" %>
                    </li>
                </ul>
            <% end %>
        </div>
    </div>
</nav>